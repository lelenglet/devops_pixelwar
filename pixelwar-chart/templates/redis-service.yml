apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: {{ .Values.namespace | default "default" | quote }}
spec:
  selector:
    app: redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379